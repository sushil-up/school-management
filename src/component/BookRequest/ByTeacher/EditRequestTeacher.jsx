import { Container, Modal } from "@mui/joy";
import React from "react";

const EditRequestTeacher = ({ handleClose, open }) => {
  return (
    <>
      <Container>
        <Modal open={open} onClose={handleClose}>

        </Modal>
      </Container>
    </>
  );
};
export default EditRequestTeacher;
